# 战场多目标识别方案分析

## 问题定义

从单一兵种图像分类扩展到战场场景的完整识别，需要同时完成:

1. **目标检测**: 识别战场上所有可互动的目标单位，返回其在战场网格上的坐标
2. **兵种分类**: 对每个检测到的目标单位判断其具体兵种类型
3. **数值识别**: 读取每个单位的数量和血量条 (数量在单位下方，血条在数量格子下方)

## 约束条件

- 英雄无敌3是回合制游戏，整个识别+决策流程的延迟在 **1秒以内** 均为合理
- 战场分辨率: 1062×664 (窗口模式)
- 战场为六边形网格，共 187 个格子 (11×17的六边形排列)

## 方案分析

### 方案A: 三模型流水线 (推荐)

```
输入截图
    │
    ├─→ [目标检测模型] → 各单位边界框 + 战场坐标
    │       │
    │       ├─→ [兵种分类模型] → 每个框的兵种类型
    │       │
    │       └─→ [OCR模型] → 每个单位的数量 + 血量
    │
    └─→ 结构化战场状态输出
```

#### 各模块详述

##### A1. 目标检测模型

**任务**: 在战场截图中定位所有单位的位置，输出边界框 + 战场坐标。

**候选方案**:

| 模型 | 参数量 | 推理速度(CPU) | 优势 |
|------|--------|--------------|------|
| YOLOv8-nano | 3.2M | ~5ms | 极快，小目标检测好 |
| YOLOv8-small | 11.1M | ~10ms | 精度更高 |
| MobileNet-SSD | 6.8M | ~8ms | 轻量，移动端友好 |
| EfficientDet-D0 | 4.0M | ~15ms | 效率高，多尺度 |

**推荐**: YOLOv8-nano (3.2M参数, CPU推理~5ms, 精度足够)

**战场坐标映射**:
- 已知六边形网格中心坐标 (gamedata/hex_centers.json)
- 检测到的边界框中心点 → 最近六边形中心 → 战场坐标
- 容差: 单位居中于六边形内，映射精度高

**训练数据需求**:
- 标注战场截图，标记每个单位的边界框 + 战场坐标
- 每个阵营、每种单位至少20-50张标注截图
- 不同战场地形背景 (草地、雪地、沙漠等)

##### A2. 兵种分类模型

**任务**: 对裁剪出的单位子图进行细粒度兵种分类。

**方案**: 复用当前训练的兵种分类模型 (EfficientNet-B0, 171类)

**输入**: 目标检测裁剪出的 58×64px 至 224×224px 单位肖像
**输出**: 171类兵种标签

**挑战**:
- 战场中的单位可能被遮挡、缩小、被法术效果覆盖
- 同阵营单位外观相似度高 (如 Pikeman vs Halberdier)
- 需要战场数据微调 (当前训练数据是肖像图，非战场图)

##### A3. OCR数值识别模型

**任务**: 识别单位下方的数量数字和血量条比例。

**数量识别**:
- 位置: 单位正下方，白色字体，固定字号
- 范围: 1-9999 (通常 1-999)
- 方案: 经典OCR (Tesseract + 预处理) 或轻量CRNN

**血量识别**:
- 位置: 数量格子下方，绿色/黄色/红色条形
- 方案: 图像处理计算血条像素宽度比例 (无需深度学习)
  ```python
  # 伪代码
  hp_region = crop_below_number(box)
  green_pixels = count_color(hp_region, GREEN_RANGE)
  total_bar_width = detect_bar_width(hp_region)
  hp_ratio = green_pixels / total_bar_width
  hp_absolute = hp_ratio * unit_max_hp
  ```

**推荐OCR方案**:

| 方案 | 延迟 | 准确率 | 复杂度 |
|------|------|--------|--------|
| Tesseract OCR | ~10ms | 95%+ | 低 (预处理: 二值化+放大) |
| 轻量CRNN | ~8ms | 98%+ | 中 (需训练) |
| 图像模板匹配 | ~2ms | 90%+ | 最低 (预存数字模板) |

**推荐**: Tesseract OCR + 预处理 (二值化 → 放大2x → OCR)，简单可靠

### 方案B: 单模型端到端

使用单个目标检测模型同时完成检测+分类，如YOLOv8直接输出171类+坐标。

**优势**:
- 单一模型，部署简单
- 检测和分类联合优化

**劣势**:
- 训练数据需求更大 (需要大量标注战场截图)
- 171类细粒度分类在检测中精度可能不足
- 无法复用已训练的兵种分类模型
- 数值识别仍需独立处理

### 方案C: 两阶段方案 (检测+属性)

使用目标检测模型定位单位+兵种分类，OCR处理数值。

**流程**: 与方案A类似但合并A1+A2为单一检测+分类模型。

### 方案对比

| 维度 | 方案A (三模型) | 方案B (端到端) | 方案C (检测+OCR) |
|------|---------------|---------------|-----------------|
| 识别延迟 | ~15ms+5ms+8ms≈28ms | ~15ms | ~15ms+8ms≈23ms |
| 准确率潜力 | 高 (各模型专精) | 中 (171类挑战) | 中-高 |
| 训练复杂度 | 中 (3个独立模型) | 高 (大量标注数据) | 中 |
| 可维护性 | 高 (模块独立升级) | 低 (需重新训练全部) | 高 |
| 复用现有模型 | 能 (兵种分类模型) | 不能 | 部分 |
| 数据需求 | 中 (各有侧重) | 非常高 | 中-高 |

## 推荐方案: 方案A (三模型流水线)

### 理由

1. **模块化**: 各模型独立训练、优化、替换，互不影响
2. **复用现有工作**: 兵种分类模型可直接复用当前的训练成果
3. **精度可控**: 每个环节精度可独立验证，问题定位清晰
4. **延迟达标**: 总延迟约28ms << 1000ms限制
5. **易于迭代**: 可以逐步改进每个模块，不需要推翻重来

### 实施路线

#### 阶段1: 目标检测 (先实现)

1. 收集50-100张战场截图，标注所有单位边界框
2. 使用YOLOv8-nano训练目标检测模型
3. 实现边界框→战场坐标映射 (基于hex_centers.json)
4. 验证检测准确率 >95%

#### 阶段2: 兵种分类 (部分已有)

1. 使用已训练的EfficientNet-B0模型
2. 从战场截图中裁剪出单位子图，测试分类准确率
3. 如果准确率不足，使用战场裁切图微调模型
4. 验证战场场景分类准确率 >85%

#### 阶段3: 数值识别 (轻量实现)

1. 实现血量条检测 (图像处理，无需模型)
2. 实现数字OCR (Tesseract + 预处理)
3. 验证数字准确率 >98%，血量准确率 >95%

#### 阶段4: 集成与优化

1. 三模型流水线串联
2. 端到端延迟优化 (多线程/异步)
3. 边界情况处理 (单位死亡、召唤、法术效果等)

## 可能的其他方案探讨

### 方案D: 分步截图法

不直接做多目标检测，而是利用游戏本身的信息:

1. 右键点击每个单位 → 游戏弹出信息框
2. 识别信息框内容 (兵种名、数量、攻防等)
3. 遍历所有单位收集完整信息

**优势**: 信息准确 (游戏内数据)，无需复杂视觉模型
**劣势**: 需要操控游戏 (鼠标点击)，耗时长 (每个单位~0.5s, 14个单位≈7s), 超过1s限制

### 方案E: 内存读取法

直接读取游戏内存/进程数据获取完整战场状态。

**优势**: 100%准确，无延迟
**劣势**: 
- 技术难度高 (逆向工程)
- 可能违反游戏使用条款
- 游戏更新后需要重新定位内存地址

**结论**: 不推荐，但可作为长期探索方向。

## 战场坐标映射说明

英雄无敌3战场为六边形网格，排列如下:

```
Row 0: 6 个格子 (偏移 0)
Row 1: 7 个格子 (偏移 0.5)
Row 2: 8 个格子 (偏移 0)
...
Row 10: 6 个格子 (偏移 0)
```

共 11 行 × 最大17列 = 187个可能位置。实际战场宽度取决于地形。

坐标映射核心代码:
```python
def pixel_to_hex(x: int, y: int, hex_centers: dict) -> tuple[int, int]:
    """将像素坐标映射到六边形网格坐标。"""
    min_dist = float("inf")
    best_hex = (0, 0)
    for key, (cx, cy) in hex_centers.items():
        dist = (x - cx) ** 2 + (y - cy) ** 2
        if dist < min_dist:
            min_dist = dist
            row, col = key.split("_")
            best_hex = (int(row), int(col))
    return best_hex
```

## 总结

推荐方案A (三模型流水线):
- **目标检测**: YOLOv8-nano (~5ms, ~3.2M参数)
- **兵种分类**: EfficientNet-B0 (~12ms, ~5.3M参数, 复用现有模型)
- **数值识别**: Tesseract OCR + 图像处理血条 (~10ms)

总推理延迟: ~28ms，远在1秒限制之内。模块化设计便于迭代维护。

下一步需要:
1. 收集标注战场截图 (用于目标检测训练)
2. 测试现有兵种分类模型在战场裁剪图上的准确率
3. 实现血条和数字识别的原型
